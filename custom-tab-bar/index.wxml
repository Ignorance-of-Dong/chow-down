<view class="tab-bar">
  <view 
    wx:for="{{list}}" 
    wx:key="pagePath" 
    class="tab-bar-item {{selected === index ? 'active' : ''}}"
    data-index="{{index}}"
    bindtap="switchTab"
  >
    <text>{{item.text}}</text>
  </view>
</view>
